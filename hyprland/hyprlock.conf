#  __  __   __  __   ______  ______   __       ______   ______   __  __
# /\ \_\ \ /\ \_\ \ /\  == \/\  == \ /\ \     /\  __ \ /\  ___\ /\ \/ /
# \ \  __ \\ \____ \\ \  _-/\ \  __< \ \ \____\ \ \/\ \\ \ \____\ \  _"-.
#  \ \_\ \_\\/\_____\\ \_\   \ \_\ \_\\ \_____\\ \_____\\ \_____\\ \_\ \_\
#   \/_/\/_/ \/_____/ \/_/    \/_/ /_/ \/_____/ \/_____/ \/_____/ \/_/\/_/

source = ~/.config/hypr/colors.conf

$font = CaskaydiaMono Nerd Font Mono

general {
  hide_cursor = false
}

animations {
  enabled = true
  bezier = linear, 1, 1, 0, 0
  animation = fadeIn, 1, 5, linear
  animation = fadeOut, 1, 5, linear
  animation = inputFieldDots, 1, 2, linear
}

background {
  monitor = desc:Xiaomi Corporation Mi Monitor 5745300002903
  path = /mnt/files/pictures/wallpapers/Matrix.png
  # blur_passes = 1
  color = rgb($background)
}

input-field {
  monitor = desc:Xiaomi Corporation Mi Monitor 5745300002903
  size = 20%, 5%
  outline_thickness = 3
  inner_color = rgba(0, 0, 0, 0.0) # no fill

  outer_color = rgb($white)
  check_color = rgb($green)
  fail_color = rgb($red)

  font_color = rgb($white_light)
  fade_on_empty = false
  rounding = 15

  font_family = $font
  placeholder_text = input password...
  fail_text = $PAMFAIL

  # Uncomment to use a letter instead of a dot to indicate the typed password
  # dots_text_format = *
  # dots_size = 0.4
  dots_spacing = 0.3

  # Uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
  # hide_input = true

  position = 0, -20
  halign = center
  valign = center
}

# ----------------------------------- TIME ----------------------------------- #

label {
  monitor = desc:Xiaomi Corporation Mi Monitor 5745300002903
  text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
  color = rgb($foreground)
  font_size = 90
  font_family = $font

  position = -30, 0
  halign = right
  valign = top
}

# ----------------------------------- DATE ----------------------------------- #

label {
  monitor = desc:Xiaomi Corporation Mi Monitor 5745300002903
  text = cmd[update:60000] date +"%A, %d %B %Y" # Update every 60 seconds
  color = rgb($foreground)
  font_size = 25
  font_family = $font

  position = -30, -150
  halign = right
  valign = top
}
