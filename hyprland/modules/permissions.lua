-- Hyprland permissions are disabled by default. Uncomment this block if you
-- want Hyprland to ask/allow/deny compositor-sensitive actions explicitly.

-- hl.config({
--   ecosystem = {
--     enforce_permissions = true,
--   },
-- })

-- hl.permission({
--   binary = "/usr/(bin|local/bin)/grim",
--   type = "screencopy",
--   mode = "allow",
-- })

-- hl.permission({
--   binary = "/usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland",
--   type = "screencopy",
--   mode = "allow",
-- })

-- hl.permission({
--   binary = "/usr/(bin|local/bin)/hyprpm",
--   type = "plugin",
--   mode = "allow",
-- })
